# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_INVOICES_BUCKET=invoices

# Mistral AI Configuration
MISTRAL_API_KEY=your-mistral-api-key

# API Security
# Generate a secure random string for production (e.g., using: openssl rand -hex 32)
# Use different keys for dev and production environments
API_KEY=your-api-key-here

# CORS Configuration
# Development: Allow localhost and dev frontend URL
# CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-app-dev.vercel.app
# Production: Allow only production frontend URL
# CORS_ALLOW_ORIGINS=https://your-app.vercel.app
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:3001
